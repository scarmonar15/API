<%= form_for(@learnings_student) do |f| %>
  <% if @learnings_student.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@learnings_student.errors.count, "error") %> prohibited this learnings_student from being saved:</h2>

      <ul>
      <% @learnings_student.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :learning_id %><br>
    <%= f.select :learning_id, Learning.all.map {|l| [l.name, l.id]} %>
  </div>
  <div class="field">
    <%= f.label :student_id %><br>
    <%= f.select :student_id, Student.all.map {|s| [s.full_name, s.id]} %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
